# API配置管理器 - 隐私保护说明

## 概述

API配置管理器是一个SillyTavern扩展，用于管理多个API配置。本文档详细说明了扩展的隐私保护机制。

## 数据存储位置

### 酒馆用户设置
- **存储位置**: 使用 SillyTavern 的 `extension_settings`，随用户设置保存到您使用的酒馆服务器，并不只存在浏览器中。
- **存储内容**: 配置名称、分组、来源、API URL、密钥、反代信息、首选模型及美化选择。导出和备份的设置可能包含明文凭据，请勿公开分享。
- **连接结果**: 只保留在当前页面内存中，不随用户设置保存；刷新后重置。

### 连接时的网络请求
- “获取模型”、“连接”与“全部连接”会把当前配置的端点和所需凭据提交给酒馆的 `/api/backends/chat-completions/status`，由酒馆向配置的服务请求模型列表。连接不是离线操作。
- 只支持 Custom / Google AI Studio 的模型列表连接，不发送聊天记录、测试消息或生成请求，也不向扩展作者发送凭据。
- 获取模型和连接不遍历、写入或轮换活动密钥，不切换当前 API；无密钥的 Custom 显式使用空授权，不借用其他配置的密钥。
- 失败状态不展示上游错误正文或凭据。停止操作取消排队并终止前端等待；已经转发的模型列表请求可能仍会完成。
- 扩展自带的更新请求和自动检查更新已移除。

### 不会上传到Git
- 配置数据**不会**包含在扩展的源代码中
- 当您将扩展发布到Git时，只会包含代码文件，不包含任何用户数据
- 其他用户安装您的扩展时，会得到一个空白的配置管理器

## 隐私保护机制

### 1. 用户设置隔离
```javascript
// 数据随此酒馆用户的 extension_settings 保存
extension_settings[MODULE_NAME] = {
    configs: [] // 用户的配置数据
};
```

### 2. 界面隐私保护
- **不显示敏感信息**: 扩展列表中不显示API URL和密钥
- **只显示必要信息**: 展示配置名称、分组、来源、模型和连接状态
- **密钥保护**: 编辑时 API 密钥与反代密码默认隐藏；只有主动点击小眼睛才显示输入框中的值。关闭弹窗、离开编辑页、切换配置/来源或保存后会重新隐藏，显隐状态不保存。小眼睛不请求或读取酒馆服务器上的其他密钥。

### 3. 代码分离
- **配置数据**: 保存在酒馆用户设置中，不包含在扩展源码中
- **扩展代码**: 只包含功能逻辑，不包含任何配置数据
- **Git发布**: 仅分享扩展源码时不会包含运行时配置；仍需检查提交内容，不要加入设置导出、备份或真实凭据

## 安全边界

### 分享扩展时
- 运行时配置与源码分离，发布源码不等于发布酒馆用户设置。
- 提交前仍需检查文件，不要把真实密钥、设置导出或备份放入仓库。

### 使用扩展时
- 密码输入框只隐藏显示，并不表示保存的凭据已加密。
- 请使用可信的酒馆服务器和端点，并保护好酒馆账号、设置文件及备份。
- 端点连通不等于模型生成一定可用；此功能不通过发送生成请求验证权限或余额。

## 文件结构

```
api-config-manager/
├── index.js          # 主要功能代码 (会上传到Git)
├── settings.html     # 界面模板 (会上传到Git)
├── style.css         # 样式文件 (会上传到Git)
├── manifest.json     # 扩展清单 (会上传到Git)
├── README.md         # 说明文档 (会上传到Git)
├── PRIVACY.md        # 隐私说明 (会上传到Git)
└── [用户配置数据]    # 位于酒馆用户设置，不属于扩展源码
```

## 使用建议

### 发布扩展到Git
1. 确保只提交代码文件
2. 不要提交任何包含配置数据的文件
3. 在`.gitignore`中排除配置文件（如果有的话）

### 安装他人的扩展
1. 通过Git URL安装是安全的
2. 您会得到一个干净的扩展，没有他人的配置
3. 需要重新添加您自己的API配置

## 技术实现

### 数据隔离
```javascript
// 每个用户的数据都是独立的
const MODULE_NAME = 'api-config-manager';
const userConfigs = extension_settings[MODULE_NAME]?.configs || [];
```

### 界面保护
```javascript
// 不显示敏感信息
const configItem = $(`
    <div class="api-config-item">
        <div class="api-config-info">
            <div class="api-config-name">${config.name}</div>
            ${config.model ? 
                `<div class="api-config-model">首选模型: ${config.model}</div>` : 
                '<div class="api-config-no-model">未设置模型</div>'
            }
        </div>
        <!-- 不显示URL和密钥 -->
    </div>
`);
```

## 常见问题

**Q: 我的API密钥会被泄露吗？**
A: 扩展源码不包含运行时密钥，但酒馆用户设置、备份以及连接请求中可能包含所需凭据。不要公开分享设置备份，只使用可信的服务器和端点。

**Q: 其他人安装我的扩展会看到我的配置吗？**
A: 不会。他们只会得到扩展的功能代码，需要添加自己的配置。

**Q: 我可以安全地分享扩展代码吗？**
A: 可以。扩展代码不包含任何用户数据，可以安全分享。

**Q: 如何备份我的配置？**
A: 可通过 SillyTavern 的设置导出功能备份 `extension_settings`；备份可能含有明文密钥，需要妥善保管。

## 总结

运行时配置与扩展源码分离；连接只读取端点模型列表，不发送生成请求。凭据仍需要交给您使用的酒馆服务器和目标服务，请谨慎管理服务器访问权限、配置导出与备份。
